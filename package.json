{
  "name": "rosmaro-redux",
  "version": "0.0.4",
  "description": "Connects Rosmaro, Redux and Redux-Saga.",
  "main": "dist/index.js",
  "scripts": {
    "test": "npx jest --watch",
    "build": "rm -rf dist && babel src -d dist --ignore '**/*.test.js'",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "rosmaro",
    "redux",
    "redux-saga",
    "fsm",
    "finite state machine"
  ],
  "author": "Łukasz Makuch <kontakt@lukaszmakuch.pl> (https://lukaszmakuch.pl)",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-syntax-object-rest-spread": "^6.13.0",
    "babel-plugin-transform-object-rest-spread": "^6.23.0",
    "babel-preset-env": "^1.4.0"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "redux": "^4.0.0",
    "redux-saga": "1.x.x"
  },
  "peerDependencies": {
    "redux-saga": "1.x.x"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
